# Linux things

Tunel ssh

´´´ssh -L 8443:bitbucket.personal.corp:8443 fmsusr@sr-docker-xd02´´´

8443 = puerto local
bitbucket.personal.corp:8443 = el server que queremos acceder
fmsusr@sr-docker-xd02 = el server que hace de intermediario